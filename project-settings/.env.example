# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=crime_system

# DSN Template (must be expanded — no ${...})
DSN=host=postgres port=5432 user=postgres password=password dbname=crime_system sslmode=disable timezone=UTC connect_timeout=5

# Secret (no quotes)
SECRET=this is a very secret key - james

BROKER_SERVICE_URL=https://broker.james-guan.info